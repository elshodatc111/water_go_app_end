import 'package:flutter/material.dart';
class OfertaPage extends StatelessWidget {
  const OfertaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Oferta"),),
    );
  }
}
